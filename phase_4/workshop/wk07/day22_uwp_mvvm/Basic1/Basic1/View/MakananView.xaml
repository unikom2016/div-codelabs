<Page
    x:Class="Basic1.View.MakananView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Basic1.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView Name="ListViewMakanan" DataContext="listmakanan">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <RelativePanel>
                        <Image Width="150" Height="150" Name="gambarMakanan" Source="{Binding gambar}"></Image>
                        <TextBlock Name="namaMakanan" 
                                   RelativePanel.RightOf="gambarMakanan" 
                                   Margin="8 0 0 0" 
                                   Text="{Binding nama}"></TextBlock>
                        <TextBlock Name="deskripsiMakanan" 
                                   RelativePanel.RightOf="gambarMakanan" 
                                   RelativePanel.Below="namaMakanan" 
                                   Margin="8 0 0 0" 
                                   Text="{Binding deskripsi}"></TextBlock>
                    </RelativePanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
