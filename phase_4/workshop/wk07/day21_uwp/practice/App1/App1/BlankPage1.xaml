<Page
    x:Class="App1.BlankPage1"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView Name="ListViewMakanan" DataContext="listMakanan">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <RelativePanel>
                        <Image Width="150" Height="150" Name="gambarMakanan" Source="{Binding gambar}"></Image>
                        <TextBlock Text="{Binding nama}"  Name="namaMakanan" RelativePanel.RightOf="gambarMakanan" Margin="8 0 0 0"></TextBlock>
                        <TextBlock Name="deskripsiMakanan" Text="{Binding deskripsi}" RelativePanel.RightOf="gambarMakanan" Margin="8 0 0 0"></TextBlock>
                    </RelativePanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
